app:
  admin:
    email: "gendalf-test-admin@white.com"
spring:
  application:
    name: web-test
  docker:
    compose:
      skip:
        in-tests: false
      profiles:
        active: test
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create-drop
